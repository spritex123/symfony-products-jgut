<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Enabled</title>
</head>
<body>
    <h1>Enabled</h1>
    {% if enabled is empty %}
        No "enabled"
    {% else %}
        <a href="{{ enabled }}">Enable</a>
    {% endif %}
</body>
</html>

